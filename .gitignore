# Bank exports and everything derived from them — never commit these
statements/*
!statements/.gitkeep
budget.md
uncategorised.csv
sample/budget.md
sample/uncategorised.csv
ledger.csv
sample/ledger.csv
.pytest_cache/

# Your personal configuration: names, dates, amounts. Copy the
# *.example.yml files to make yours; only the examples are tracked.
# Anchored to the root on purpose: sample/*.yml is the invented household
# and must be tracked.
/rules.yml
/loans.yml
/known-annual.yml

__pycache__/
*.pyc
.pixi/*
!.pixi/config.toml
.DS_Store

# the browser build and its download cache
web/dist/
web/.cache/
